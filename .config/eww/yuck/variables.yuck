;; Music Variables
(deflisten song	"scripts/music title")
(deflisten song_artist "scripts/music artist")
(deflisten song_status "scripts/music status")
(deflisten song_perc "scripts/music percent")
(deflisten cover_art "scripts/music coverloc")
(deflisten playpause "scripts/music statusicon")
(deflisten player_volume 'bc <<< "$(playerctl volume)*100"')

(defvar music_reveal false)
